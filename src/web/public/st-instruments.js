/**
 * Instruments surface: placeholder. Filled in by a later task; the file exists
 * now so the namespace shape and the asset manifest are stable.
 */
(function (ST) {
  "use strict";

  ST.instruments = {};
})(window.Steamtrain);
